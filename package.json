{
  "name": "mr-white",
  "version": "0.1.0",
  "private": true,
  "description": "Everyone gets the same secret word. One player gets nothing at all, and has to work out what everyone else is talking about before they are caught. Play online with friends.",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node tools/test.js",
    "smoke": "node tools/smoke.js",
    "verify": "npm test && npm run smoke"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "ws": "^8.18.0"
  },
  "optionalDependencies": {
    "playwright": "^1.62.0"
  }
}
